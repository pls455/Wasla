name: wasla
publish_to: 'none'
version: 1.0.0+1
description: Wasla - private direct device-to-device file transfer.
environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^12.2.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
