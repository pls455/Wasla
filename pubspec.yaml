name: wasla
publish_to: 'none'
version: 1.2.0+3
description: Wasla - fast private direct device-to-device sharing.
environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^10.3.10
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  crypto: ^3.0.7
  qr_flutter: ^4.1.0
  mobile_scanner: ^6.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
